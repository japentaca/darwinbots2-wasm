# Preguntas abiertas

Todo lo marcado `[SIN VERIFICAR]` en los documentos de `spec/`, centralizado.
**Ninguna de estas se responde con el wiki.** Se responden leyendo el fuente, o quedan
abiertas.

Estado: `ABIERTA` · `EN CURSO` · `RESUELTA (cita)` · `IRRESOLUBLE DESDE EL FUENTE`

---

## Desde Fase 0 (reconocimiento)

| # | Pregunta | Origen | Estado |
|---|---|---|---|
| Q01 | ¿Qué llamadas a `Rnd`/`Randomize` fuera de `Common.bas:rndy` ocurren **dentro** del ciclo de simulación? Alteran el orden de consumo del flujo aleatorio y por tanto los replays. Sospechosos: `Obstacles.bas` (10 usos), `main.frm` (15), `Globals.bas` (1), `HDRoutines.bas` (2). **Parcial desde A1**: dentro del tick consumen RNG `VegsRepopulate`/`aggiungirob` (`Vegs.bas:32`, `Globals.bas:411-415`), `feedvegs` (`Vegs.bas:51-52`), `feedveg2` (`Vegs.bas:282`), `BrownianForces` (`Physics.bas:113-117`), el intérprete (`DNA.bas:277`, `:1098`), `ReproduceAndKill` (`Robots.bas:1668`) y las mutaciones. **Precisado en A2**: en el intérprete son exactamente los opcodes `rnd` y `rndstore`, 1 extracción cada uno y solo cuando ejecutan (`20-VM.md §14`); `RobScriptLoad` añade 6 por bot cargado (ver Q12). **Ampliada en A3** (`21-MEMORIA.md §11`): fudge de refvars con `FudgeEyes`/`FudgeAll` (1 por campo trucado, `Senses.bas:237-299`, `Ties.bas:743-796`), `altzheimer` (≥2 por iteración con re-tiradas, `Robots.bas:991-995`), remapeo aleatorio de `Vloc`/`Ploc` con re-tiradas hasta ≠340 (`Shots.bas:806-808,851-853`; `Ties.bas:398-401,466-469,604-607`), `newshot` (2 por disparo, `Shots.bas:130,146`), `Vshoot` (1, `:1106`), `maketie` (1, `Ties.bas:898`), `addgene` (1, `Shots.bas:1194`), `Reproduce` gate vegetal (1, `Robots.bas:2129`), `loadrobs` (1 por fundador, `main.frm:1556`). **Completada en B**: `Obstacles.bas` solo consume con drift activado (2/eje/forma, `50-MUNDO.md §4`); `HDRoutines.bas` no consume dentro del tick (`Rnd` crudo solo en carga/torneo: `SimGUID`, `60-FORMATOS.md §4`); añadidos teleporters (drift/local/entrada), `Decay` de corpses, `newshot` (2, uno muerto), crossover (~1/segmento), mutaciones (~6·DnaLen por parto), `mutatecolors`, fudge. Los únicos `Rnd` fuera de `rndy` en el motor son setup/carga/UI. | `00-INVENTARIO.md §6`, `20-VM.md §14`, `21-MEMORIA.md §11`, `50-MUNDO.md §8`, `60-FORMATOS.md §8` | RESUELTA (inventario completo) |
| Q02 | ¿Qué algoritmo exacto usa el `Rnd` de VB6 y con qué semilla arranca? Determina si el port puede reproducir secuencias bit a bit. **Resuelta con fuente externa** (2026-08-16; el EXE no corre en el Windows del proyecto, se levantó la veda de fuentes secundarias para las preguntas de runtime). Autoridad: Microsoft — `Microsoft.VisualBasic.Core/VBMath.vb` y `ProjectData.vb` en [dotnet/runtime](https://github.com/dotnet/runtime/blob/main/src/libraries/Microsoft.VisualBasic.Core/src/Microsoft/VisualBasic/VBMath.vb), que reimplementan el runtime VB6 citando el C++ original (`rtRandomNext`, `RandomizeTimer`, `RandomizeValue`), coherente con MS KB231847. Algoritmo: estado de 24 bits, semilla inicial del proceso `&H50000` (327680). `Rnd()`/`Rnd(n>0)`: `seed = (seed*&H43FD43FD + &HC39EC3) And &HFFFFFF`; devuelve `CSng(seed)/16777216` (por tanto `0 <= Rnd < 1`, y `Rnd=0` es alcanzable). `Rnd(0)`: devuelve el último valor sin avanzar. `Rnd(n<0)`: reinterpreta los bits del `Single` como entero `b` y fija `seed = (b + (b >> 24)) And &HFFFFFF` antes de avanzar una vez. `Randomize n`: toma los bits IEEE de `n` (como `Double`, palabra alta), hace `mix = ((bits And &HFFFF) Xor (bits >> 16)) << 8` y **solo reemplaza los bytes medios**: `seed = (seed And &HFF0000FF) Or mix` — el byte bajo del estado anterior sobrevive. `Randomize` sin argumento usa `Timer` (segundos desde medianoche, `Single`). En el motor: la vía determinista es `Randomize SimOpts.UserSeedNumber / 100` al arrancar sim (`main.frm:1198,1392`); con proceso fresco (`seed=&H50000`, byte bajo 0) el estado post-Randomize es una función pura del seed del usuario. `rndy` (`Common.bas:228-254`) con `UseIntRnd=False` es `Rnd` a secas; con `UseIntRnd=True` re-siembra cada 1..1500 extracciones desde `rndylist` (fuente de internet — el port puede omitir este modo). **Decisión de port**: implementar este LCG bit a bit (aritmética entera de 32 bits, máscara 2²⁴); la reproducción de secuencias es exacta por construcción. | `00-INVENTARIO.md §6` | RESUELTA (fuente externa: MS) |
| Q03 | ¿Algún array del motor usa el índice 0 con significado? No hay `Option Base` en ningún archivo, pero el código itera `1 To MaxRobs`. **Parcial desde A1**: `rob(0)` nunca se puebla, pero es alcanzable vía `KillRobot(0)` desde la matanza por presión de memoria (`10-CICLO.md §8`). **Cerrada para `mem` en A3**: `mem(0)` recibe escrituras del motor — es el sumidero deliberado de venom/poison cuando la dirección del atacante mapea a 340 (`Poisons`, `Robots.bas:1118,1127`; remapeo en `Shots.bas:804,849`, `Ties.bas:396,464`); nunca se lee; se persiste en los saves (`21-MEMORIA.md §6`). **Barrido completado en B**: `Shots(0)` no se usa (`33-SHOTS.md §8`); `Ties(0)` no se usa y `Ties(10)` es un slot fantasma alcanzable solo por corrimiento/`TieTorque` (`34-TIES.md §6`); `Buckets().arr(0)` no se usa (`32-VISION.md §8`); `rob(0)` tiene dos usos reales: `KillRobot(0)` (A1 §8) y scratch de `SetDefaultMutationRates` con `NormMut` (UI, `40-MUTACIONES.md §8`). | `10-CICLO.md §8`, `21-MEMORIA.md §6`, `33-SHOTS.md §8`, `34-TIES.md §6`, `40-MUTACIONES.md §8` | RESUELTA |
| Q04 | ¿El modo consola (`console.frm:466`) produce una secuencia de ciclo distinta a la del bucle de `main.frm:2078`? | `00-INVENTARIO.md §5` | RESUELTA (`console.frm:461-480` llama al mismo `UpdateSim`; sólo cambia el render, incondicional por ciclo. `10-CICLO.md §1`) |
| Q05 | ¿Qué contiene `NodeSpeedThings.bas` (203 LOC, código muerto) y por qué se dejó? Puede documentar una intención de diseño abandonada. | `00-INVENTARIO.md §3` | RESUELTA (100% comentado: el índice espacial `Roborder` ordenado por X del sistema de visión pre-buckets; sus consumidores son los `proximity`/`Checkleft/right` también comentados de `Senses.bas:519-740`. `50-MUNDO.md §0.5`) |
| Q06 | ¿`Main.bas` (502 LOC) define un `Sub Main` alternativo para el runner de tests? No está en el EXE, sólo en `UnitTests`. | `00-INVENTARIO.md §3` | RESUELTA (no hay `Sub Main`: declaraciones Win32 y subclassing de ventanas — `WindowProc`/`Hook`/`UnHook`, registro. Dependencia de UI del runner, no punto de entrada. `60-FORMATOS.md §0.6`) |
| Q07 | Impacto real de los flags de FP sobre la reproducibilidad numérica: ¿hay acumuladores `Single` donde la precisión extendida x87 cambie el resultado observable? **Avanzada en B1**: todo el estado físico es `Single` con guardas manuales de underflow (1e-7) y formulación estable de magnitudes — inventario en `30-FISICA.md §8`. **Cerrada como decisión de port** (2026-08-16, tras la corrección de flags de `00-INVENTARIO.md §1`): *(a)* `FlPointCheck=0` = chequeo FP **activo** — `GravityForces` con `PhysMoving=0` da error 11 + truncamiento, no un valor x87; *(b)* `UnroundedFP=0` = el compilador **redondea a la precisión declarada en cada asignación** — la única divergencia posible frente a IEEE 754 estricto es el doble redondeo de intermedios x87 de 80 bits **dentro de una misma expresión** (y las diferencias de librería en `Sqr`/`Atn`/`Sin`); es del orden de 1 ulp por expresión, no falsable sin el binario (Q09) y sin efecto estructural gracias a las guardas manuales inventariadas. **Decisión de port**: IEEE 754 binary32 estricto por operación (lo que da WASM; sin `-ffast-math`, sin FMA implícita, promociones a `double` solo donde el fuente las tiene), guardas de underflow replicadas tal cual, y determinismo bit a bit **del port consigo mismo** entre plataformas como requisito (WASM lo garantiza salvo payloads de NaN, que el motor no produce). Los casos dorados de física del Bloque C se definen con tolerancia relativa, no bit a bit contra el original. | `00-INVENTARIO.md §1`, `30-FISICA.md §8` | RESUELTA (decisión de port) |
| Q08 | ¿Divergen el EXE compilado y el IDE en algún punto **observable** por culpa de los flags de compilación? **Ampliada en A2** (`20-VM.md §14`) con el inventario de sitios alcanzables desde ADN de bot. **Cerrada — la pregunta se disuelve** (2026-08-16): la corrección de flags (`00-INVENTARIO.md §1`) establece que el EXE compila **con** todos los chequeos — EXE ≈ IDE, no hay divergencia por chequeos; la residual numérica x87 queda absorbida por Q07. El inventario de A2 sigue siendo valioso con otra lectura: son los **sitios de error runtime alcanzables desde ADN**, cada uno con su efecto real ya especificado — literal fuera de ±32767 o `def` 1001+ → el archivo **no carga** (`20-VM.md §2.4, §8.1`); ADN vacío tras defs → "bomba de tick" (error 9 cada ciclo, `§2.3`); `add`/`sub` cerca de ±2³¹ → error 6 + truncamiento (`§6.1`, Q17); `NextStop` sin bounds → error 9 capturado o truncamiento (`§10`). El port define comportamiento explícito en cada sitio (documentado en cada sección). | `00-INVENTARIO.md §1`, `20-VM.md §14`, `10-CICLO.md §14` | RESUELTA (disuelta por corrección de flags) |
| Q09 | El árbol llegó **con los finales de línea normalizados**: 68 de los 69 fuentes VB6 del motor tienen LF; sólo `TrayIcon.cls` conserva CRLF, que es lo que VB6 escribe. ¿Carga el IDE de VB6 archivos sin CRLF? Si no, hace falta un paso de conversión para poder abrir `Iersera.vbp` y observar el comportamiento real. No afecta a las citas `archivo:línea`. **Cerrada 2026-08-16**: ni el EXE original ni el IDE de VB6 funcionan en la máquina del proyecto (Windows 11), confirmado por el usuario. La validación empírica contra el binario queda descartada como vía; las preguntas que dependían de ella (Q07, Q08, Q17) se cierran con fuentes secundarias y decisiones de port explícitas. Queda como nota de infraestructura: si alguna vez se monta una VM con VB6, convertir los fuentes a CRLF **en una copia**, nunca en `Darwinbots2/`. | verificación de `.gitattributes`, commit `ec0dc6d` | IRRESOLUBLE EN ESTE ENTORNO |

---

## Desde A1 (`10-CICLO.md`)

La pregunta central del bloque —simultánea o secuencial— quedó **respondida** en
`10-CICLO.md §0`: secuencial, in situ, sin doble búfer, con efectos de orden enumerados.

| # | Pregunta | Origen | Estado |
|---|---|---|---|
| Q10 | Mecánica interna de los teleporters (`CheckTeleporters` en la pasada P0a, `UpdateTeleporters` en el paso 18 del tick): ¿crean/destruyen bots, en qué orden, consumen RNG? | `10-CICLO.md §6` | RESUELTA (sí a todo: salida = serializar organismo a disco + `KillOrganism` en P0a; entrada = sondeo de directorio + `LoadOrganism` en el paso 18; local = `ReSpawn` aleatorio; RNG: drift 1-2/teleporter/ciclo, 2/evento local, 2/organismo entrante internet. `50-MUNDO.md §3`) |
| Q11 | `UpdateTieAngles` corre sobre slots inexistentes (`Robots.bas:1621`, antes del chequeo `exist`). ¿Tiene efecto observable sobre un slot en blanco? | `10-CICLO.md §11.6` | RESUELTA (solo escribe `mem(450)=mem(451)=0` del slot fantasma — `Ties.bas:87-88` — y `posto` borra el slot entero al reutilizarlo, `Robots.bas:2962-2963`. Sin efecto observable. `21-MEMORIA.md §9.9`) |
| Q12 | ¿`RobScriptLoad` consume RNG al cargar un vegetal desde disco durante `VegsRepopulate`? Afecta a los replays cada vez que repuebla. | `10-CICLO.md §12` | RESUELTA (sí: 6 extracciones de `rndy` vía `preparerob` — `pos.x`, `pos.y`, `aim`, 3 colores; `Module1.bas:32-43`. `20-VM.md §2.1`) |
| Q13 | Desborde teórico de `rep(32000)` con población máxima y doble encolado asexual+sexual (`Robots.bas:1391-1400`): ¿es alcanzable en la práctica y qué pisa en el EXE (`BoundsCheck=0`)? **Cerrada por análisis** (2026-08-16). Alcanzabilidad: `rep(0 To 32000)` (32001 celdas, `Robots.bas:365-367`); `rp = 1` se resetea al inicio de cada `UpdateBots` (`Robots.bas:1487`), así que el desborde exige ≥32001 encolamientos **en un mismo ciclo**: cada bot existente aporta ≤2 (asexual `:1391-1394` + sexual `:1397-1400` — el comentario `:1366` reconoce el doble encolado). Hacen falta ≥16001 bots todos doblemente encolados (o ≥32001 encolamientos mixtos) en el mismo tick. La población **no** tiene tope duro: `SimOpts.MaxPopulation` solo frena vegetales vía `TotalChlr` (`Robots.bas:2123,2450`), y `posto()` agranda `rob()` sin límite en incrementos de 100 (`Robots.bas:2929-2943`; el comentario `:2950` admite que falta el cap). Conclusión: **alcanzable solo en escenarios degenerados** (≥16k bots reproduciéndose a la vez); impracticable en ecología normal (memoria/CPU colapsan antes). Qué hace el EXE *(corregido 2026-08-16 — chequeos activos, `00-INVENTARIO.md §1`)*: el encolamiento 32002.º lanza **error 9** y trunca el resto del tick (`10-CICLO.md §14`); no pisa nada. **Decisión de port**: cola de reproducción dimensionada a 2×capacidad de bots (nunca desborda); el truncamiento del original en este caso degenerado queda fuera de la spec de compatibilidad. | `10-CICLO.md §6` | RESUELTA (análisis) |
| Q14 | ¿`GiveAbsNum`/`AbsNum` (identidad absoluta de bots, `Robots.bas:2966`) tienen overflow alcanzable en simulaciones largas? `parent` se compara contra `AbsNum` (`Robots.bas:2234`, `Shots.bas:330`). **Parte A3 resuelta**: `AbsNum` **no se publica en memoria** en ninguna parte; `GiveAbsNum` incrementa `SimOpts.MaxAbsNum As Long` +1 por nacimiento (`HDRoutines.bas:1587-1599`, `SimOptions.bas:187`) — overflow impracticable (2³¹ nacimientos). **Cerrada en B**: `MaxAbsNum` se persiste con la sim (`HDRoutines.bas:759,1417-1418`); las comparaciones `parent`/`AbsNum` están documentadas — y la de `Shots.bas:330` resultó ser **la inmunidad filial rota** (compara slot contra AbsNum, `33-SHOTS.md §0.2`); los `.dbo` importados pueden traer AbsNum duplicados sin deduplicar (`60-FORMATOS.md §2`). | `10-CICLO.md §12`, `21-MEMORIA.md §0.5`, `33-SHOTS.md §0.2`, `60-FORMATOS.md §2` | RESUELTA |

---

## Desde A2 (`20-VM.md`)

| # | Pregunta | Origen | Estado |
|---|---|---|---|
| Q15 | ¿Algún subsistema fuera del intérprete escribe en `mem()` valores fuera de ±32000 (shots con tipo positivo, senses, ties)? Habilitaría el overflow `Integer` de `absstore`/`negstore` sobre −32768 (`20-VM.md §7`). | `20-VM.md §13` | RESUELTA (`21-MEMORIA.md §7`: en juego normal, no — todo camino del motor está acotado a ±32000. Excepciones: `mem(220)`/`mem(715)` = `Kills` sin clamp en la vía de shots (`Shots.bas:595,713`; `Senses.bas:327`) — 32001..32767 kills quedan sobre 32000; con ≥32768 la asignación lanza error 6 y trunca el tick *(corregido 2026-08-16 — chequeos activos)*, así que −32768 solo puede entrar en `mem` desde saves con `Integer` crudo; el fudge de modos evo deja 32001 (`Senses.bas:237-299`); y la carga de saves/consola escriben Integer crudos.) |
| Q16 | `calc_dnamatrix`/`DNAtoInt` (`DNATokenizing.bas:15-56`): el comentario dice 76 comandos, el conteo estático de tokens detokenizables da 77. ¿Cuál es el tamaño real de la matriz en runtime y afecta a `Point2`/`CopyError2`? | `20-VM.md §9` | RESUELTA (77 comandos, índices 0..76 — el "76" del comentario es el índice máximo; `DNAtoInt` llega justo a 32767 sin overflow; `ismutating` excluye `debugint`/`debugbool` en ambas direcciones, así que las mutaciones sondean Max=12 para tipo 3. No afecta a Point2/CE2, que usan las tablas sysvar. `40-MUTACIONES.md §0.4, §5`) |
| Q17 | ¿Qué valor exacto produce el EXE cuando una conversión `Single`→`Long` desborda (p. ej. `Mod` en `add`/`sub` con operandos ≈ ±2³¹)? Hipótesis original: −2³¹ ("integer indefinite" x87). **Resuelta en dos pasos el 2026-08-16.** *(i)* Primer cierre (obsoleto en el mismo día): con la premisa "checks eliminados", el `fistp` con invalid enmascarada almacenaría el *integer indefinite* `0x80000000` ([Intel SDM, FIST/FISTP](https://www.felixcloutier.com/x86/fist:fistp)). *(ii)* **Cierre definitivo tras la corrección de flags** (`00-INVENTARIO.md §1`: los flags `=0` del `.vbp` son casillas sin marcar — chequeos ACTIVOS): la conversión desbordada lanza **error 6 también en el EXE**; no se produce ningún valor. Sin handler en `DNA.bas`, el error desenrolla hasta `main()` y **trunca el resto del tick** (`10-CICLO.md §14`). La hipótesis −2³¹ era doblemente errónea: premisa invertida, y el push nunca ocurre. **Decisión de port**: detectar el caso explícitamente (redondeo bancario + test de rango int32) y definir un comportamiento documentado (recomendado en `20-VM.md §6.1`: saturar a ±2·10⁹ como el resto de caminos de `add`/`sub`), registrando la divergencia con el original (que truncaba el tick). | `20-VM.md §6.1, §13` | RESUELTA (corrección de flags) |
